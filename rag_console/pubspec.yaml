name: rag_console
description: RAG Console.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  archive: ^3.4.5
  console:
    path: ../console
  dio: ^5.3.3
  document:
    path: ../document
  flutter:
    sdk: flutter
  surrealdb_wasm:
    path: ../../surrealdb_wasm

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
